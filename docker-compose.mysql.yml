# task_feishu/docker-compose.mysql.yml

version: '3.8'

services:
  # Backend service
  app:
    build:
      context: .
      dockerfile: Dockerfile.mysql
    ports:
      - "8000:8000"  # Expose backend API directly for testing, can be removed later
    environment:
      # 飞书应用配置 (请在部署时替换为真实的值)
      - FEISHU_APP_ID=${FEISHU_APP_ID}
      - FEISHU_APP_SECRET=${FEISHU_APP_SECRET}
      - FEISHU_APP_TOKEN=${FEISHU_APP_TOKEN}
      - FEISHU_TABLE_ID=${FEISHU_TABLE_ID}
      # MySQL 数据库配置
      - DB_HOST=192.168.101.13
      - DB_PORT=3306
      - DB_USER=root
      - DB_PASSWORD=1qaz2wsx
      - DB_NAME=feishu_task_db
      # 其他环境变量
      - BACKEND_PORT=8000
    volumes:
      # 挂载卷以使用宿主机上修改后的filter_config.json文件
      - ./backend/filter_config.json:/app/filter_config.json
    # 重启策略
    restart: unless-stopped
    # 依赖 MySQL 服务
    # depends_on:
      # - mysql

  # Frontend service with Nginx
  frontend:
    build:
      context: .
      dockerfile: dd/Dockerfile.frontend
    ports:
      - "8080:80"  # Expose Nginx on port 8080 to avoid conflict with host's port 80
    depends_on:
      - app
    # 重启策略
    restart: unless-stopped

  # MySQL database service
  # mysql:
    # image: mysql:latest
    # environment:
      # MYSQL_ROOT_PASSWORD: 1qaz2wsx
      # MYSQL_DATABASE: feishu_task_db
    # ports:
      # - "3307:3306"  # Expose MySQL on port 3307 to avoid conflict with existing MySQL container
    # volumes:
      # - mysql_data:/var/lib/mysql
    # 重启策略
    # restart: unless-stopped

# 定义数据卷
volumes:
  mysql_data:
